version: '3.8'

services:
    app:
        build:
            context: .
            dockerfile: Dockerfile
        image: routebox-dev:latest
        ports:
            - "8080:8080"
        environment:
            SPRING_PROFILES_ACTIVE: dev
            TZ: Asia/Seoul
        restart: always
